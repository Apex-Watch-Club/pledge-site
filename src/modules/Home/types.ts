type BenefitPairType = {
  heading: string;
  description: string;
};

type ERC20DescriptorType = {
  address: string;
  name: string;
  symbol: string;
  icon: string;
};

export type { ERC20DescriptorType, BenefitPairType };
